{
  "name": "transformerpath-site",
  "version": "1.0.0",
  "private": true,
  "description": "TransformerPath — static site build scripts and Netlify serverless-function dependencies. @supabase/supabase-js is required by the serverless functions (functions/lib/supabase-server.js) and is bundled with esbuild by Netlify.",
  "scripts": {
    "test": "node tests/check-p0-integrity.js && node tests/check-account-schema.js && node tests/topology-integrity.test.js && node tests/grid-calculations.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  }
}
