{
  "name": "transformerpath",
  "version": "1.0.0",
  "private": true,
  "description": "TransformerPath — transformer parts directory, 3D explorers, intel, and monetization (static site + Netlify functions).",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "start": "node scripts/dev-server.js",
    "build:stats": "node scripts/build-stats.js",
    "smoke": "node scripts/smoke-part-coverage.js",
    "search": "node scripts/search-index.js",
    "search:smoke": "node scripts/search-smoke.js",
    "search:precision": "node scripts/check-search-precision.js",
    "stripe:test": "node scripts/check-stripe-fulfillment.js",
    "health": "node scripts/directory-health.js",
    "gates": "node scripts/gates.js",
    "test": "node scripts/gates.js"
  },
  "dependencies": {
    "stripe": "^14.25.0"
  },
  "engines": {
    "node": ">=18"
  }
}
